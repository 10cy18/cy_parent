server:
  port: 9001
spring:
  application:
    name: cy-base
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/cy_base?characterEncoding=utf-8
    username: root
    password:
  jpa:
    database: mysql
    show-sql: true
    generate-ddl: true
eureka:
  client:
    service‐url:
      defaultZone: http://localhost:6868/eureka
  instance:
    prefer‐ip‐address: true